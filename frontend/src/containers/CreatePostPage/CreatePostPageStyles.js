import {makeStyles} from "@material-ui/styles";

const CreatePostPageStyles = makeStyles({
    main: {
        height: "90vh",
        background: "#fff",
        padding: "20px",
        justifyContent: "center"
    },
    form: {
        width: "30%"
    }
});

export default CreatePostPageStyles